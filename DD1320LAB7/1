from hash_table import HashTable

def main():
    length = 20000
    my_table: HashTable[str] = HashTable(1)
    for i in range(length):
        my_table.store(f"hello{i}", "word")

    for node in my_table.node_store:
        if node is not None:
            print(node.key, hash(node))

    i = 0
    j = 0

    for i in range(length):
        key = f"hello{i}"
        try:
            my_table[key]
            j += 1
        except KeyError:
            i += 1
            print("did not find", key)
    print(j)


if __name__ == "__main__":
    main()
